interface:
  display_name: "Gene-to-Reference Tree"
  short_description: "Build auditable protein trees with exact taxonomy"
  default_prompt: "Use $bio-gene-to-reference-tree to resolve my protein, validate exact NCBI TaxIDs when local taxdump files are available, curate references and outgroups, and plan an annotated, literature-aware gene tree with iTOL or local ggtree output."
